streamlit==1.22.0       # compatible with scikit-learn 1.6.1
flask==3.1.1            # latest Flask, should be fine
pandas==2.1.0           # stable version compatible with scikit-learn
numpy==1.26.0           # stable version compatible with scikit-learn
scikit-learn==1.6.1     # matches the version used to train the model
joblib==1.5.1           # matches the version used to save the model
